export function Time(hour, minute) {

    const MAX_HOUR = 24;
    const MAX_MINUTE = 59;
    const MIN = 0;

    const _hour = parseInt(hour);
    const _minute = parseInt(minute);

    this.isGreater = (hour, minute) => {
        
    }


}